# Should be used twice, first to store, then to copy
[[processors.gtc_processor]]
  # namepass to limit the incoming metrics
  # general plugin property
  # namepass = ["container_*", "kube_*"]

  # service label name to add
  # label = "service"
  # refresh interval of pod-service info (minutes)
  # refresh_interval = 1
  # time to keep pod-service info in cache after no longer present (minutes)
  # cache_duration = 30
