# Should be used twice, first to store, then to copy
[[processors.gtc_join_service]]
  # namepass to limit the incoming metrics
  # general plugin property
  # namepass = ["kube_pod_annotations"]

  # One of store or copy should be true

  # store to extract the service label and store it linked to pod label
  # store = false

  # copy service label to metrics depending on their pod label
  # copy = false
